# Forced env vars (ensure_env_vars): bewusst leer — alles Projektspezifische
# ist seed-bar (config/.env.seed), damit Projekte z. B. XO_WORDPRESS_ROOT
# verschieben können (etwa wenn ./wordpress ein Subrepo mit dem Theme ist).
