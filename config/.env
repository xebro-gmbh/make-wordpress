# Forced env vars (ensure_env_vars): intentionally empty — everything
# project-specific is seedable (config/.env.seed), so projects can relocate
# e.g. XO_WORDPRESS_ROOT (say, when ./wordpress is a subrepo with the theme).
